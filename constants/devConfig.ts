// Phase 3 dev mode flag — true in `expo start`, false in production build.
// This is the single switch for dev-only features (e.g. admin upgrade button).
// Do NOT use EXPO_PUBLIC_DEV_MODE or other env vars for this purpose.
export const DEV_MODE = __DEV__;
